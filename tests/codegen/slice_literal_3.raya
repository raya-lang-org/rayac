// expect_exit: 15
fn sum(data: []i64) -> i64 {
    var total: i64 = 0;
    for i: i64 in 0..data.len {
        total = total + data[i];
    }
    return total;
}

fn main() -> i64 {
    const arr = []i64{1, 2, 3, 4, 5};
    return sum(arr);
}
