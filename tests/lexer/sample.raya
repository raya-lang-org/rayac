#[serializable]
pub struct Vec3 {
    x: f32,
    y: f32,
    z: f32,
}

extend Vec3 with Printable {
    pub fn print(self: &const Vec3) -> void {
        const msg = "Vec3";
    }
}

pub fn add(a: f32, b: f32) -> f32 {
    return a + b;
}
